# @nativescript/android-shermes

NativeScript runtime package for Android, built with the Static Hermes (shermes) JavaScript engine.

## Build

From the repo root:

```sh
./gradlew -Pengine=SHERMES
```

This produces the npm artifact in `dist_shermes/`.
